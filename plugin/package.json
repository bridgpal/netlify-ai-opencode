{
  "name": "opencode-netlify-ai",
  "version": "0.2.0",
  "description": "OpenCode v2 provider plugin for Netlify's AI Gateway, loaded locally from this repo (not published to npm)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "node check.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@opencode/plugin": "2.0.14"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true
}
